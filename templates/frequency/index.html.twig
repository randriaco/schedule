{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(formFreq) }}
        <div class="d-flex justify-content-center">
            <div class="row">
                <div class="col-10">
                    {{ form_widget(formFreq) }}
                </div>
                <div class="col-2">
                    <p><br>min</p>
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-center mt-2">
            <button class="btn btn-primary">Valider</button>
        </div>
    {{ form_end(formFreq) }}

{% endblock %}